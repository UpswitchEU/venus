/**
 * Fields that have a dedicated input + SpotlightFieldWrapper on the streamlined calculator.
 * Flags on other Hermes aggregates still need visible anchors — see GuidedResolutionOrphanFields.
 */
export const SPOTLIGHT_QUICK_FORM_FIELDS = new Set(['revenue', 'ebitda', 'industry'])
